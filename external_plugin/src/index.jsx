import React from "react";
import ReactDOM from "react-dom/client";
import TableApp from "./pages/TableApp/TableApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
   <React.StrictMode>
      <TableApp />
   </React.StrictMode>
);
